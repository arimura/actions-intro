name: CI

on: 
  push:
    branches:
      - master

jobs:
  build:
    runs-on: macOS-latest
    steps:
    - name: pod install
      run: make install
    - name: start testing
      run: make PLATFORM=gha test

