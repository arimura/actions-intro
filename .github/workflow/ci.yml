name: CI

on:
  push:
    branches:
      - '**'  # Runs on push to any branch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: check envs 
        run: make check 
